        <!-- 工厂稿与工厂打样稿照片 -->
        <view class="cell-collapse">
          <t-collapse>
            <t-collapse-panel value="0">
              <view slot="header">
                <text class="title-font-main">工厂打样稿：</text>
              </view>
              <view class="cell-swiper">
                <t-swiper wx:if="{{swiperImages.length !== 0 }}" current="{{current}}" autoplay="{{autoplay}}" duration="{{duration}}" interval="{{interval}}" navigation="{{ { type: 'dots' } }}" list="{{swiperImages}}" bind:click="onSwiperImagesTap" data-images="{{swiperImages}}">
                </t-swiper>
                <!-- 无数据时显示占位内容 -->
                <t-image wx:else src="" shape="round" error="slot" width="640rpx" height="384rpx">
                  <text class="error-text" slot="error">轮播图加载失败</text>
                </t-image>
              </view>
            </t-collapse-panel>
            <t-collapse-panel value="1">
              <view slot="header">
                <text class="title-font-main">样品照片：</text>
              </view>
              <view class="cell-swiper">
                <t-swiper wx:if="{{swiperImages.length !== 0 }}" current="{{current}}" autoplay="{{autoplay}}" duration="{{duration}}" interval="{{interval}}" navigation="{{ { type: 'dots' } }}" list="{{swiperImages}}" bind:click="onSwiperImagesTap" data-images="{{swiperImages}}">
                </t-swiper>
                <!-- 无数据时显示占位内容 -->
                <t-image wx:else src="" shape="round" error="slot" width="640rpx" height="384rpx">
                  <text class="error-text" slot="error">轮播图加载失败</text>
                </t-image>
              </view>
            </t-collapse-panel>
          </t-collapse>
        </view>